{
  "name": "floxen",
  "version": "1.0.0",
  "private": true,
  "description": "Floxen monorepo (API + Web) following the technical specification.",
  "license": "ISC",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm --workspace apps/api run dev",
    "dev:web": "npm --workspace apps/web run dev",
    "dev:landing": "npm --workspace apps/landing run dev",
    "dev:test-gen": "npm --workspace apps/test-generator run dev",
    "dev:product-checker": "npm --workspace apps/product-checker run dev",
    "build": "npm run build:shared && npm run build:api && npm run build:web && npm run build:landing && npm run build:product-checker",
    "build:shared": "npm --workspace packages/shared run build",
    "build:api": "npm --workspace apps/api run build",
    "build:web": "npm --workspace apps/web run build",
    "build:landing": "npm --workspace apps/landing run build",
    "build:test-gen": "npm --workspace apps/test-generator run build",
    "build:product-checker": "npm --workspace apps/product-checker run build",
    "start": "node scripts/start.js",
    "start:api": "npm --workspace apps/api run start",
    "start:landing": "npm --workspace apps/landing run start",
    "start:product-checker": "npm --workspace apps/product-checker run start",
    "migrate:deploy": "cd apps/api && npx prisma migrate deploy",
    "lint": "npm --workspaces run lint",
    "format": "npm --workspaces run format",
    "test": "npm --workspaces run test"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "eslint": "^9.39.2",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.53.0"
  }
}
